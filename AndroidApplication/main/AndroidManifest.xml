<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carapp">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
       <!--  <activity android:name=".View.Reports"></activity> -->
        <activity android:name=".View.adminProfile" />
        <activity android:name=".View.DriverList" />
        <activity android:name=".View.AddDriver" />
        <activity android:name=".View.driverProfile" />
        <activity android:name=".View.AdminActivity" />
        <activity android:name=".View.DriverActivity" />
        <activity android:name=".View.Reports" />
        <activity android:name=".distance" />
        <activity android:name=".bumps" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>